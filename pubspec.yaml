name: flutter_map_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # --- Flutter Map versi terbaru (WAJIB) ---
  flutter_map: ^8.2.2
  latlong2: ^0.9.0
  # -----------------------------------------

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Jika ingin menambah gambar/icons, gunakan:
  # assets:
  #   - assets/images/
  #   - assets/icons/
